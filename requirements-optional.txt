# Dépendances optionnelles pour les méthodes de chunking avancées

# Pour chunking par tokens (CHUNK_METHOD="token")
transformers>=4.30.0

# Pour chunking sémantique (CHUNK_METHOD="semantic")
sentence-transformers>=2.2.0

# Pour chunking LangChain (CHUNK_METHOD="langchain")
langchain>=0.1.0
langchain-text-splitters>=0.0.1
